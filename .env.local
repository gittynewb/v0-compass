# Do not commit real API keys here.
# Set GEMINI_API_KEY in your Vercel Environment Variables for production.
# For local development, create a .env.local file with your real key (never commit it).
GEMINI_API_KEY=your_gemini_api_key_here
